# Infrastructure Tests
## Tests project
This project has a Test stack which should never get deployed to any environment. This stack is useful for testing the Base nuget package locally.
Test it by running a `cdk ls` or `cdk synth` from the `tests` folder.

