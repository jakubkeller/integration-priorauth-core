variables:
  SERVICE_CONNECTION: "integration-dev" #TODO: Update with the correct service connection for production
  ENVIRONMENTTYPE: "production"  # variable name must match name used in BaseTaggingOptions.cs, value must match AWS tagging policy
  pauseNotify: |
    aws.utilizationmanagement+PROD@navitus.com
