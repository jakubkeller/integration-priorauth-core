variables:
  SERVICE_CONNECTION: "integration-stage"
  ENVIRONMENTTYPE: "stage"  # variable name must match name used in BaseTaggingOptions.cs, value must match AWS tagging policy
  pauseNotify: |
    aws.utilizationmanagement+Stage@navitus.com